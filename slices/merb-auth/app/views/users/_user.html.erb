<tr>
	<th><%= link_to user.login, url(:user, user) %></th>
	<td><%= user.name %></td>
	<td><%= user.email %></td>
	<td><%= user.time_zone %></td>
	<td> <%= link_to "Edit", url(:edit_user, user) %> | <%= link_to 'Delete', url(:delete_user, user), {:method => :delete, :onclick => "return confirm('Are you sure?')"} %></td>
</tr>